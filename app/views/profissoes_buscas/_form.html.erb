<%= form_for(@profissoes_busca) do |f| %>
  <% if @profissoes_busca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profissoes_busca.errors.count, "error") %> prohibited this profissoes_busca from being saved:</h2>

      <ul>
      <% @profissoes_busca.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
